#!/bin/bash
# Project-local MCP context loader for Claude Code hooks.
# Invoked by .claude/settings.json on SessionStart and PostCompact.
set -e
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
python3 "${SCRIPT_DIR}/mcp_context_loader.py"
